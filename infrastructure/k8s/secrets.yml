apiVersion: v1
kind: Secret
metadata:
  name: shadowsig-secrets
type: Opaque
stringData:
  # Replace with actual secure values before applying
  POSTGRES_USER: "shadowsig"
  POSTGRES_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME"
